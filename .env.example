# Copiá este archivo con el nombre ".env" y completá los dos valores.
# Los encontrás en: Supabase → tu proyecto → Project Settings → API
#
# Mientras este archivo .env NO exista, la app corre en "modo demo"
# (todo se guarda en el navegador, sin cuentas reales).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
